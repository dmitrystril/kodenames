gameState = {
  cards: [
    {
      id,
      type: Red | Blue | White | Black,
      isActive,
    },
    ...
  ],
  score,
  users,
  userTeam: Enum?,
  whoseMove,
}

USER - stores only profile-specific data:
email, username, password, avatar, phone, etc

PLAYER - stores gameplay specific data:
room, game, team
