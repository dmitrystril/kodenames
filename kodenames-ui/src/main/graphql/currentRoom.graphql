query currentRoom {
  currentRoom {
    id
    no
    game {
      id
      cards {
        id
        no
        type
        word
        isOpen
        isActive
      }
      players {
        id
        team
      }
      dateCreated
    }
  }
}
